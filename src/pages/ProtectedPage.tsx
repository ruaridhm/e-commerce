import { Typography } from '@mui/material';
import Layout from '../components/Layout';

export default function ProtectedPage() {
  return (
    <Layout>
      <Typography variant='h2'>Protected page</Typography>
    </Layout>
  );
}
